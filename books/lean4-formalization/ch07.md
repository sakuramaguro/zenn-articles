---
title: "第7章：距離空間という抽象化"
---

# 第7章：距離空間という抽象化 〜ℝを忘れて、`dist`だけで世界を作る〜

完成例は本書の固定環境（Lean 4.29.0-rc6）で検証しています。`import Mathlib` から始まる例は、それぞれ別ファイルで実行してください。分割した証明は指定した順に連結します。表示した型・ゴールは必要な部分の要約で、画面画像は執筆時の記録です。

---

## 7.0 本章のゴールを先に見せる ──── ゴール逆算型アプローチ

[第6章](ch06)の終わりで、著者は「`nhds a` の基底が `Metric.ball a δ`（$\delta$-開球）だ」と言いました。その章では近傍との対応を確認しましたが、距離空間の構造自体はまだ詳しく扱っていません。`Metric.ball` とは何か。`dist` とは何か。`MetricSpace` とは何か。

本章ではそれらを正面から解剖します。ただし、今回もまず**本章の最終ゴール**を先に見せておきます。

```lean:完成例・確認コマンド
-- 本章の最終ゴール（これを証明できるようになることが目標）
import Mathlib

-- 【勝利の命題①】開球は開集合である
theorem open_ball_preview (X : Type*) [MetricSpace X] (x : X) (r : ℝ) :
    IsOpen (Metric.ball x r) := by
  exact Metric.isOpen_ball

-- 【勝利の命題②】点を除かない極限の ε-δ による特徴付け（b = f a なら連続性）
theorem tendsto_epsilon_delta_preview (f : ℝ → ℝ) (a b : ℝ) :
    Filter.Tendsto f (nhds a) (nhds b) ↔
    ∀ ε > 0, ∃ δ > 0, ∀ x, dist x a < δ → dist (f x) b < ε := by
  exact Metric.tendsto_nhds_nhds
```

どちらも一行で通ります。しかし「なぜ通るのか」を理解するには、`MetricSpace` という**距離空間の免許証**が何者かを知る必要があります。

---

## 7.1 距離の公理 ──── 数学の定義をLeanに翻訳する

### 数学の定義：距離の3公理

集合 $X$ 上の **距離（metric）** とは、関数 $d : X \times X \to \mathbb{R}$ であって次の3条件を満たすものです。

$$
\text{（D1：正定値性）}\quad d(x, y) \geq 0 \quad \text{かつ} \quad d(x, y) = 0 \iff x = y
$$

$$
\text{（D2：対称律）}\quad d(x, y) = d(y, x)
$$

$$
\text{（D3：三角不等式）}\quad d(x, z) \leq d(x, y) + d(y, z)
$$

![3点x、y、zを結ぶ距離の三角不等式](/images/vol2/fig-04.png)
*図7-1：直接の距離は、別の点を経由した距離の和以下です。*

この3条件が距離の定義を与えます。D1は「距離は非負で、距離がゼロであることと同じ点であることが同値」、D2は「AからBの距離はBからAの距離と同じ」、D3は「回り道をすると遠くなる」という直感です。

### Leanの実装：`MetricSpace` 型クラスの解剖

Lean では距離空間を **型クラス（型の免許証）** として実装しています。Part 1で学んだ「型クラス ＝ 免許証」の比喩を思い出してください。`MetricSpace X` は距離関数とその法則、対応する位相などをまとめた構造です。`[MetricSpace X]` でその構造を前提として受け取り、標準的な型では登録済みのインスタンスを利用できます。

```lean:完成例・確認コマンド
import Mathlib

-- 固定版 Mathlib のフィールドを実際に表示する
#print PseudoMetricSpace
#print MetricSpace
#check @dist_self
#check @eq_of_dist_eq_zero
```

固定版の `PseudoMetricSpace` は `Dist` を継承し、`dist_self`・`dist_comm`・`dist_triangle` を持ちます。非負性 `dist_nonneg` はこれらから導かれる補題で、別の公理フィールドではありません。実際、$0=d(x,x)\le d(x,y)+d(y,x)=2d(x,y)$ から非負性が従います。

実装には拡張距離 `edist`、一様構造 `UniformSpace`、有界性の構造 `Bornology` と距離との整合条件もあります。既定値があるため、すべてを手で構成する必要はありません。`MetricSpace` はさらに `eq_of_dist_eq_zero` を要求します。数学の公理との対応は次のとおりです。

| 数学の公理 | 数式 | Lean のフィールド・補題 |
|-----------|------|----------------------|
| D1（非負性） | $d(x, y) \geq 0$ | `dist_nonneg : 0 ≤ dist x y` |
| D1（零距離 → 同一点） | $d(x, y) = 0 \Rightarrow x = y$ | `eq_of_dist_eq_zero` |
| D1（同一点 → 零距離） | $d(x, x) = 0$ | `dist_self : dist x x = 0` |
| D2（対称律） | $d(x, y) = d(y, x)$ | `dist_comm : dist x y = dist y x` |
| D3（三角不等式） | $d(x, z) \leq d(x, y) + d(y, z)$ | `dist_triangle : dist x z ≤ dist x y + dist y z` |

```lean:完成例・確認コマンド
import Mathlib

-- 実際に #check で確認
#check @dist_nonneg
-- dist_nonneg : ∀ {α : Type u_1} [inst : PseudoMetricSpace α] {x y : α}, 0 ≤ dist x y

#check @dist_comm
-- dist_comm : ∀ {α : Type u_1} [inst : PseudoMetricSpace α] (x y : α),
--   dist x y = dist y x

#check @dist_triangle
-- dist_triangle : ∀ {α : Type u_1} [inst : PseudoMetricSpace α] (x y z : α),
--   dist x z ≤ dist x y + dist y z
```

:::message
**💡 `PseudoMetricSpace` と `MetricSpace` の違い**

`PseudoMetricSpace` は D1の後半「$d(x,y) = 0 \Rightarrow x = y$」を要求しません。つまり「距離がゼロでも同一点とは限らない」擬距離空間を許容します。`MetricSpace` はそこに `eq_of_dist_eq_zero` を追加した、より強い構造です。

例えば二乗可積分な可測関数を点ごとの等号で区別すると、異なる関数でもほとんど至る所等しければ $L^2$ の擬距離は0になります。そこで、距離0の関数を同一視した商を取ると、通常の距離空間 $L^2$ が得られます。商を取る前と後を区別しましょう。[第10章](ch10)の数列空間 $\ell^2$ も、通常の距離空間です。
:::

### 具体例：ℝ上の距離

最も身近な距離空間、実数 $\mathbb{R}$ の距離を確認します。

```lean:完成例・確認コマンド
import Mathlib

-- ℝ は MetricSpace のインスタンス
#check (inferInstance : MetricSpace ℝ)

-- ℝ での dist は絶対値
#check Real.dist_eq
-- Real.dist_eq : dist x y = |x - y|

-- 実際に計算
theorem real_distance : dist (3 : ℝ) 5 = 2 := by
  rw [Real.dist_eq]
  -- ⊢ |3 - 5| = 2  →  norm_num で解決
  norm_num

-- 三角不等式の確認
theorem real_triangle (x y z : ℝ) : dist x z ≤ dist x y + dist y z :=
  dist_triangle x y z  -- 公理そのまま
```

:::message
**🔍 InfoView：`dist (3 : ℝ) 5 = 2` の証明**

`rw [Real.dist_eq]` 直後：

```text
⊢ |3 - 5| = 2
```

`norm_num` で数値計算が完了します。`dist` という抽象的な記号が `|·|` に展開され、具体的な計算が可能になる——これが `Real.dist_eq` の役割です。
:::

---

## 7.2 `Metric.ball` の正体 ──── 開球という武器

[第6章](ch06)で予告した `Metric.ball` を、いよいよ正面から見ていきます。

### 数学の定義：開球と閉球

距離空間 $(X, d)$ の点 $x \in X$ と実数 $r$ に対して、開球と閉球を次で定めます。通常は $r>0$ を考えますが、Leanの定義では半径を任意の実数にできます。

$$
B(x, r) = \{ y \in X \mid d(x, y) < r \} \quad \text{（開球・open ball）}
$$

$$
\bar{B}(x, r) = \{ y \in X \mid d(x, y) \leq r \} \quad \text{（閉球・closed ball）}
$$

![中心xから距離ε未満の点を集めたユークリッド平面の開球](/images/vol2/fig-05.png)
*図7-2：平面の開球を描いた模式図です。円周上の点は含みません。閉球では距離が半径以下の点を含めます。*

### Leanの実装

```lean:完成例・確認コマンド
import Mathlib

-- 数学：B(x, r) = { y | d(x, y) < r }
-- Lean：Metric.ball x r
#check @Metric.ball
-- Metric.ball : α → ℝ → Set α
-- Metric.ball x r = {y | dist y x < r}

-- 数学：B̄(x, r) = { y | d(x, y) ≤ r }
-- Lean：Metric.closedBall x r
#check @Metric.closedBall
-- Metric.closedBall x r = {y | dist y x ≤ r}
```

:::message
**💡 引数の順序に注意**

数学では $B(x, r)$ と書いて「中心 $x$、半径 $r$」を表しますが、Lean の `{y | dist y x < r}` では **`dist y x`** の順で `y`（可変点）が先に来ます。`dist` の引数順序は「`dist (測りたい点) (中心)`」ではなく「`dist (点1) (点2)`」ですが、`Metric.ball x r` の定義内では慣習的に `dist y x`（中心が第2引数）と書かれています。実用上は `Metric.mem_ball` 補題で展開すると明確です。
:::

```lean:完成例・確認コマンド
import Mathlib

-- Metric.ball のメンバーシップ条件を確認
#check @Metric.mem_ball
-- Metric.mem_ball : y ∈ Metric.ball x r ↔ dist y x < r

-- 具体例：2 は ball 0 3 に属するか？
theorem two_mem_ball : (2 : ℝ) ∈ Metric.ball 0 3 := by
  rw [Metric.mem_ball]
  -- ⊢ dist 2 0 < 3
  norm_num [Real.dist_eq]
  -- No goals ✓
```

**ステップ①：ゴールの確認**

以下の3個のLeanブロックは、上から順に連結して実行してください。途中までは未完成の証明です。

```lean:完成例・確認コマンド
import Mathlib

theorem two_mem_ball_steps : (2 : ℝ) ∈ Metric.ball 0 3 := by
```

```text
⊢ 2 ∈ Metric.ball 0 3
```

**ステップ②：`rw [Metric.mem_ball]` で展開**

```lean:断片
  rw [Metric.mem_ball]
```

```text
⊢ dist 2 0 < 3
```

「2が ball(0, 3) に属する」という集合論的な命題が、「dist 2 0 < 3」という不等式に変換されました。

**ステップ③：数値計算で完了**

```lean:断片
  norm_num [Real.dist_eq]
  -- dist 2 0 = |2 - 0| = 2 < 3 ✓
  -- No goals ✓
```

---

## 7.3 `nhds`と`Metric.ball`の接続 ──── 第6章の謎が解ける

[第6章](ch06)で「`nhds a` の基底は `Metric.ball a ε` だ」と言いました。この接続を今こそ正確に理解しましょう。

### `HasBasis` とは何か

まず `HasBasis` の意味を確認します。

```lean:完成例・確認コマンド
import Mathlib

#check Filter.HasBasis
-- Filter.HasBasis : Filter α → (ι → Prop) → (ι → Set α) → Prop
-- f.HasBasis p s は「フィルター f の基底が {s i | p i} である」を意味する
```

「フィルター $\mathcal{F}$ が基底 $\{B_i\}_{i \in I}$ を持つ」とは：

$$
U \in \mathcal{F} \iff \exists i \in I,\; B_i \subseteq U
$$

つまり「フィルターの集合は、基底の元のいずれかを含む集合すべて」ということです。

### `Metric.nhds_basis_ball` の意味

```lean:完成例・確認コマンド
import Mathlib

#check @Metric.nhds_basis_ball
-- Metric.nhds_basis_ball :
--   (nhds x).HasBasis (fun ε => 0 < ε) (Metric.ball x)
```

これを展開すると：

$$
U \in \mathcal{N}(x) \iff \exists \varepsilon > 0,\; B(x, \varepsilon) \subseteq U
$$

つまり「$x$ の近傍 $U$ とは、ある開球 $B(x, \varepsilon)$ を含む集合」ということです。これは数学的な「近傍」の定義そのものです。

```lean:完成例・確認コマンド
import Mathlib

-- U が a の近傍であるための必要十分条件
theorem nhds_iff_ball (a : ℝ) (U : Set ℝ) :
    U ∈ nhds a ↔ ∃ ε > 0, Metric.ball a ε ⊆ U := by
  rw [Metric.nhds_basis_ball.mem_iff]
  -- No goals：同じ式の同値になるため rw が閉じる
```

**ステップ①：ゴール確認**

以下の2個のLeanブロックは、上から順に連結して実行してください。途中までは未完成の証明です。

```lean:完成例・確認コマンド
import Mathlib

theorem nhds_iff_ball_steps (a : ℝ) (U : Set ℝ) :
    U ∈ nhds a ↔ ∃ ε > 0, Metric.ball a ε ⊆ U := by
```

```text
⊢ U ∈ nhds a ↔ ∃ ε > 0, Metric.ball a ε ⊆ U
```

**ステップ②：`HasBasis.mem_iff` で展開**

```lean:断片
  rw [Metric.nhds_basis_ball.mem_iff]
```

```text
No goals
```

Lean の `∃ ε > 0, P ε` は `∃ ε, ε > 0 ∧ P ε` の記法です。この例では `rw` による書き換え後に両辺が定義的に同じになり、そのままゴールが閉じます。後ろに `rfl` や `simp` を追加すると、処理すべきゴールがないというエラーになります。

:::message
**💡 数学とLeanの完全対応：近傍の特徴付け**

| 数学の表現 | Lean の表現 |
|-----------|------------|
| $U \in \mathcal{N}(a)$ | `U ∈ nhds a` |
| $\exists \varepsilon > 0,\; B(a,\varepsilon) \subseteq U$ | `∃ ε > 0, Metric.ball a ε ⊆ U` |
| $B(a,\varepsilon) = \{x \mid d(x,a) < \varepsilon\}$ | `Metric.ball a ε = {x \| dist x a < ε}` |
| 上の同値 | `Metric.nhds_basis_ball.mem_iff` |

:::

---

## 7.4 開集合の定義と`IsOpen` ──── 位相の骨格

距離空間における「開集合」をLeanで形式化します。

### 数学の定義：距離空間における開集合

距離空間 $(X, d)$ の部分集合 $U \subseteq X$ が **開集合（open set）** であるとは：

$$
\forall x \in U,\; \exists \varepsilon > 0,\; B(x, \varepsilon) \subseteq U
$$

「$U$ の各点の周りに、$U$ に完全に含まれる開球が存在する」ということです。

開集合$U$の各点$x$には、$U$に収まる小さな開球があります。

$$
x\in U\quad\Longrightarrow\quad
\exists\varepsilon>0,\ B(x,\varepsilon)\subseteq U.
$$

### Leanの実装：`IsOpen` と `Metric.isOpen_iff`

```lean:完成例・確認コマンド
import Mathlib

-- IsOpen の定義確認
#check @IsOpen
-- IsOpen : Set α → Prop

-- 距離空間での開集合の特徴付け
#check Metric.isOpen_iff
-- Metric.isOpen_iff :
--   IsOpen s ↔ ∀ x ∈ s, ∃ ε > 0, Metric.ball x ε ⊆ s
```

数学の定義がそのまま `Metric.isOpen_iff` に翻訳されています。

| 数学の表現 | Lean の表現 |
|-----------|------------|
| $U$ は開集合 | `IsOpen U` |
| $\forall x \in U,\; \exists \varepsilon > 0,\; B(x,\varepsilon) \subseteq U$ | `∀ x ∈ U, ∃ ε > 0, Metric.ball x ε ⊆ U` |
| 上の同値 | `Metric.isOpen_iff` |

---

## 7.5 本章の勝利命題①：開球は開集合 ──── InfoViewでのゴールの変化

いよいよ本章の最初の目標、「開球 `Metric.ball x r` は開集合 `IsOpen` である」を証明します。

### 証明の方針を逆算する

ゴールは `IsOpen (Metric.ball x r)` です。これを証明するには：

1. `Metric.isOpen_iff` を適用して「任意の点 $y \in B(x, r)$ に対して、$y$ の周りに開球が存在する」に変換する
2. $y \in B(x, r)$ は $d(y, x) < r$ を意味するので、残り $r - d(y, x) > 0$ を半径にすれば良い

この `r - dist y x` が「証人」になります。

### ゴールの変化：ステップバイステップ

以下の7個のLeanブロックは、上から順に連結して実行してください。途中までは未完成の証明です。

```lean:完成例・確認コマンド
import Mathlib

-- 目標：開球は開集合
theorem open_ball_from_triangle (X : Type*) [MetricSpace X] (x : X) (r : ℝ) :
    IsOpen (Metric.ball x r) := by
```

**ステップ①：最初のゴール**

```text
X : Type*
inst✝ : MetricSpace X
x : X
r : ℝ
⊢ IsOpen (Metric.ball x r)
```

`IsOpen` の定義を展開するために `Metric.isOpen_iff` を使います。

**ステップ②：`rw [Metric.isOpen_iff]` で展開**

```lean:断片
  rw [Metric.isOpen_iff]
```

```text
⊢ ∀ y ∈ Metric.ball x r, ∃ ε > 0, Metric.ball y ε ⊆ Metric.ball x r
```

「開球の各点の周りに、より小さな開球が存在する」という形になりました。

**ステップ③：`intro y hy` で点と仮定を導入**

```lean:断片
  intro y hy
```

```text
y : X
hy : y ∈ Metric.ball x r
⊢ ∃ ε > 0, Metric.ball y ε ⊆ Metric.ball x r
```

`hy : y ∈ Metric.ball x r` を展開します。

**ステップ④：`rw [Metric.mem_ball] at hy` で仮定を数値化**

```lean:断片
  rw [Metric.mem_ball] at hy
```

```text
y : X
hy : dist y x < r
⊢ ∃ ε > 0, Metric.ball y ε ⊆ Metric.ball x r
```

`hy` が `dist y x < r` という不等式になりました。証人 `ε := r - dist y x` が見えてきます。

**ステップ⑤：証人を具体的に出す**

```lean:断片
  refine ⟨r - dist y x, by linarith, ?_⟩
```

```text
y : X
hy : dist y x < r
⊢ Metric.ball y (r - dist y x) ⊆ Metric.ball x r
```

`by linarith` が `r - dist y x > 0`（`hy` から `dist y x < r` を使って）を自動証明します。残るゴールは「小さな開球が大きな開球に含まれること」です。

**ステップ⑥：包含関係を示す**

```lean:断片
  intro z hz
  rw [Metric.mem_ball] at hz ⊢
```

```text
z : X
hz : dist z y < r - dist y x
⊢ dist z x < r
```

**ステップ⑦：三角不等式で完了**

```lean:断片
  calc dist z x ≤ dist z y + dist y x := dist_triangle z y x
    _ < (r - dist y x) + dist y x    := by linarith
    _ = r                             := by ring
  -- No goals ✓
```

証明完了です！ 三角不等式 `dist_triangle` が核心にあります。

:::message
**💡 この証明の数学的な要点**

$z \in B(y, r - d(y,x))$、つまり $d(z,y) < r - d(y,x)$ という仮定から、三角不等式によって：

$$
d(z, x) \leq d(z, y) + d(y, x) < (r - d(y,x)) + d(y,x) = r
$$

よって $z \in B(x, r)$。この証明の骨格はそのまま Lean の `calc` ブロックになっています。
:::

### Mathlib の一行バージョンと比較

```lean:完成例・確認コマンド
import Mathlib

-- 実はMathlib に既に定理がある
theorem open_ball_library (X : Type*) [MetricSpace X] (x : X) (r : ℝ) :
    IsOpen (Metric.ball x r) :=
  Metric.isOpen_ball  -- ← 一行で終わる
```

:::message alert
**手動証明で確認すること**

手動証明では、半径を $r-d(y,x)$ と選ぶ数学的な理由と、Leanの各ゴールを対応させています。この証明も `Metric.isOpen_ball` も、有限次元性は要求せず、無限次元の距離空間で使えます。後の章で閉球のコンパクト性を扱うときには、それとは別の仮定を確認します。
:::

---

## 7.6 本章の目標②：極限の特徴付け ──── Tendstoとε-δの架け橋

[第6章](ch06)では `Metric.tendsto_nhds_nhds` という定理を「ある」と言いつつ使いました。今度はこの定理が**なぜ成り立つか**を、距離空間の構造から理解します。

### 定理の再確認

```lean:完成例・確認コマンド
import Mathlib

#check @Metric.tendsto_nhds_nhds
-- Metric.tendsto_nhds_nhds :
--   Tendsto f (nhds a) (nhds b) ↔
--   ∀ ε > 0, ∃ δ > 0, ∀ ⦃x⦄, dist x a < δ → dist (f x) b < ε
```

左辺（フィルター）と右辺（ε-δ）が同値です。この式は点 $a$ を除外しない極限を表し、$b=f(a)$ とした場合が連続性です。なぜ同値になるのでしょうか。

### 証明の核心：`HasBasis.tendsto_iff`

```lean:完成例・確認コマンド
import Mathlib

-- フィルターの基底を使った Tendsto の特徴付け
#check Filter.HasBasis.tendsto_iff
-- HasBasis.tendsto_iff :
--   l.HasBasis p s → m.HasBasis q t →
--   (Tendsto f l m ↔ ∀ i, q i → ∃ j, p j ∧ ∀ x ∈ s j, f x ∈ t i)
```

これを `nhds` の基底 `Metric.ball` に適用すると：

- `l = nhds a`、基底は `Metric.ball a δ`（`δ > 0`）
- `m = nhds b`、基底は `Metric.ball b ε`（`ε > 0`）

代入すれば：

$$
\mathtt{Tendsto}\; f\; (\mathcal{N}(a))\; (\mathcal{N}(b))
\iff \forall \varepsilon > 0,\; \exists \delta > 0,\; \forall x \in B(a, \delta),\; f(x) \in B(b, \varepsilon)
$$

$$
\iff \forall \varepsilon > 0,\; \exists \delta > 0,\; \forall x,\; d(x, a) < \delta \Rightarrow d(f(x), b) < \varepsilon
$$

これがそのままε-δ定義です。

以下の2個のLeanブロックは、上から順に連結して実行してください。途中までは未完成の証明です。

```lean:完成例・確認コマンド
import Mathlib

-- 実際に Metric.tendsto_nhds_nhds を使った証明の実況
theorem tendsto_to_epsilon_delta (f : ℝ → ℝ) (a b : ℝ) (hf : Filter.Tendsto f (nhds a) (nhds b)) :
    ∀ ε > 0, ∃ δ > 0, ∀ x, dist x a < δ → dist (f x) b < ε := by
```

**ステップ①：ゴール確認**

```text
hf : Tendsto f (nhds a) (nhds b)
⊢ ∀ ε > 0, ∃ δ > 0, ∀ x, dist x a < δ → dist (f x) b < ε
```

**ステップ②：`Metric.tendsto_nhds_nhds.mp` で一発変換**

```lean:断片
  exact Metric.tendsto_nhds_nhds.mp hf
  -- No goals ✓
```

```text
-- No goals ✓
```

`Metric.tendsto_nhds_nhds` は `Iff` なので、`.mp`（modus ponens、前向き）で左辺から右辺への変換が取り出せます。

---

## 7.7 連続関数の形式化 ──── `ContinuousAt`とε-δの一本化

本章で学んだ距離空間の道具を使い、連続性のε-δ定義がLeanでどう扱われるかを総まとめします。

### 数学の定義：ε-δによる連続性

関数 $f : X \to Y$（距離空間間の写像）が点 $a$ で連続であるとは：

$$
\forall \varepsilon > 0,\; \exists \delta > 0,\; \forall x \in X,\; d_X(x, a) < \delta \Rightarrow d_Y(f(x), f(a)) < \varepsilon
$$

### Leanでの対応表

| 数学の記法 | Lean の記法 | 媒介する補題 |
|-----------|------------|------------|
| $f$ は $a$ で連続（ε-δ） | `∀ ε > 0, ∃ δ > 0, ∀ x, dist x a < δ → dist (f x) (f a) < ε` | `Metric.continuousAt_iff` |
| $f$ は $a$ で連続（位相） | `ContinuousAt f a` | `ContinuousAt` の定義 |
| $f$ は $a$ で連続（フィルター） | `Tendsto f (nhds a) (nhds (f a))` | `ContinuousAt` の定義展開 |
| 三者は同値 | ↕ | `Metric.continuousAt_iff` + `Metric.tendsto_nhds_nhds` |

```lean:完成例・確認コマンド
import Mathlib

-- 三者の同値を一気に確認
#check @Metric.continuousAt_iff
-- Metric.continuousAt_iff :
--   ContinuousAt f x ↔
--   ∀ ε > 0, ∃ δ > 0, ∀ ⦃y⦄, dist y x < δ → dist (f y) (f x) < ε
```

### 具体例：$f(x) = x^2$ の連続性

以下の2個のLeanブロックは、上から順に連結して実行してください。途中までは未完成の証明です。

```lean:完成例・確認コマンド
import Mathlib

-- x² は ℝ 上の連続関数
theorem square_continuous : Continuous (fun x : ℝ => x ^ 2) := by
```

**ステップ①：ゴール確認**

```text
⊢ Continuous (fun x => x ^ 2)
```

**ステップ②：Mathlibの連続性の定理を使う**

```lean:断片
  exact continuous_pow 2
  -- No goals ✓
```

`continuous_pow 2` は「$n$ 乗関数は連続」という定理です。定理名には `continuous_pow` のような形のほか、`Continuous.pow` のように仮定へ適用する形もあります。

```lean:完成例・確認コマンド
import Mathlib

-- もう少し手動で：恒等写像の連続性から組み立てる
theorem square_continuous_from_id : Continuous (fun x : ℝ => x ^ 2) := by
  have hid : Continuous (id : ℝ → ℝ) := continuous_id
  -- id は連続。x^2 = id * id なのでどうするか？
  exact hid.pow 2  -- Continuous.pow : Continuous f → Continuous (fun x => f x ^ n)
  -- No goals ✓
```

**ステップ①：`hid` の宣言後のゴール**

```text
hid : Continuous id
⊢ Continuous (fun x => x ^ 2)
```

**ステップ②：`hid.pow 2` で一行**

```text
-- No goals ✓
```

`hid.pow 2` は「連続関数の $n$ 乗も連続」という補題 `Continuous.pow` のドット記法です。

---

## 7.8 本章のまとめと勝利のカタルシス

本章で学んだことを整理します。

| 概念 | 数学の記法 | Lean の記法 |
|------|-----------|------------|
| 距離関数 | $d : X \times X \to \mathbb{R}$ | `dist : α → α → ℝ` |
| 距離空間 | $(X, d)$ | `[MetricSpace α]`（型クラス） |
| 非負性 | $d(x,y) \geq 0$ | `dist_nonneg` |
| 対称律 | $d(x,y) = d(y,x)$ | `dist_comm` |
| 三角不等式 | $d(x,z) \leq d(x,y)+d(y,z)$ | `dist_triangle` |
| 開球 | $B(x,r) = \{y \mid d(y,x) < r\}$ | `Metric.ball x r` |
| 閉球 | $\bar{B}(x,r) = \{y \mid d(y,x) \leq r\}$ | `Metric.closedBall x r` |
| 開集合 | $\forall x \in U, \exists \varepsilon > 0, B(x,\varepsilon) \subseteq U$ | `IsOpen U`（`Metric.isOpen_iff`） |
| 開球は開 | $B(x,r)$ は開集合 | `Metric.isOpen_ball` |
| 連続性（ε-δ） | $\forall \varepsilon > 0, \exists \delta > 0, \ldots$ | `Metric.continuousAt_iff` |
| 連続性（フィルター） | `ContinuousAt f a` | `Tendsto f (nhds a) (nhds (f a))` |

ここまでで、距離空間の開球が開集合であることと、連続性・極限のε-δによる特徴付けを扱いました。同じ定理を $\mathbb{R}$ だけでなくユークリッド空間にも適用できます。コンパクト性は次章以降の課題です。

```lean:完成例・確認コマンド
import Mathlib

-- ユークリッド空間でも同じ開球の定理を使える
theorem euclidean_ball_open (n : ℕ) (x : EuclideanSpace ℝ (Fin n)) (r : ℝ) :
    IsOpen (Metric.ball x r) :=
  Metric.isOpen_ball

theorem euclidean_continuous_tendsto
    (f : EuclideanSpace ℝ (Fin 3) → EuclideanSpace ℝ (Fin 2))
    (hf : Continuous f) (a : EuclideanSpace ℝ (Fin 3)) :
    Filter.Tendsto f (nhds a) (nhds (f a)) :=
  hf.continuousAt
```

どちらの例でも必要な構造がインスタンスとして与えられるため、具体的な座標計算をせずに定理を適用できます。一般的な型を扱う定理にも必要な仮定があるので、その仮定を確認することは欠かせません。

:::message alert
**無限次元空間では、コンパクト性の仮定を確認する**

[第10章](ch10)では**無限次元空間** $\ell^2$（絶対値の二乗が総和可能な数列の全体）を扱います。本章の開球や連続性の定理は引き続き使えますが、有限次元の場合との違いに注意します。

- **有界閉集合 $\neq$ コンパクト**：$\mathbb{R}^n$ では Heine-Borel 定理により有界閉集合はコンパクトですが、$\ell^2$ では**有界閉集合がコンパクトとは限りません**。単位閉球が非コンパクトです。
- **弱位相と強位相の乖離**：ノルム収束と弱収束が一致しなくなります。`Tendsto f (nhds a) (nhds b)` が「どの位相の `nhds` か」を明示しなければなりません。
- **完備性は別の仮定**：コーシー列の収束や不動点定理には、完備性 `CompleteSpace` を確認します。完備性を加えても、無限次元の単位閉球がコンパクトになるわけではありません。

[第8章](ch08)・[第9章](ch09)で `CompleteSpace` と完備化を学び、[第10章](ch10)で `ℓ²` の構築を実況中継します。これらの章で、距離・位相・完備性・コンパクト性の役割を区別していきます。
:::

:::message
**🔗 [第8章](ch08)への伏線**

本章で「開球は開集合」を証明しました。しかし「開集合とは何か」について、私たちはまだ距離空間の言葉（`Metric.isOpen_iff`）でしか語っていません。

距離を持たない**一般位相空間**では「開集合」はどう定義されるのでしょうか。答えは `TopologicalSpace` 型クラスにあります。次章では `IsOpen`・`IsClosed`・`IsCompact` の三角形と、距離空間から位相空間への「格下げ」がどう機能するかを解説します。
:::

---

## 章末練習問題

:::message
**📝 理解度チェック**

 **問1（数学↔Lean 対応）**

次の数学的表現をLeanの記法に翻訳してください。

- (a) 距離空間 $(X, d)$ において $d(x, y) = d(y, x)$
- (b) $B(a, \varepsilon) = \{ x \in \mathbb{R} \mid |x - a| < \varepsilon \}$
- (c) $f : \mathbb{R} \to \mathbb{R}$ が $a$ で連続（ε-δ定義）

:::details ヒントを見る

(a) は `dist_comm`、(b) は `Metric.ball`、(c) は `Metric.continuousAt_iff` または `ContinuousAt` の定義を展開して確認します。
:::

:::details 解答を見る

```lean:完成例・確認コマンド
import Mathlib

-- (a) d(x, y) = d(y, x)
#check @dist_comm
-- dist_comm : ∀ {α} [PseudoMetricSpace α] (x y : α), dist x y = dist y x
-- Lean の表現: dist x y = dist y x（補題名: dist_comm）

-- (b) B(a, ε) = {x | |x - a| < ε}
#check @Metric.ball
-- Metric.ball : α → ℝ → Set α
-- Metric.ball a ε = {x | dist x a < ε}
-- ℝ では dist x a = |x - a| なので数学の定義と一致

-- (c) f が a で連続（ε-δ 定義）
theorem continuousAt_epsilon_delta (f : ℝ → ℝ) (a : ℝ) :
    ContinuousAt f a ↔
    ∀ ε > 0, ∃ δ > 0, ∀ x, dist x a < δ → dist (f x) (f a) < ε := by
  exact Metric.continuousAt_iff
```

(c) は `Metric.continuousAt_iff` が数学的な ε-δ 定義と Lean の `ContinuousAt` の同値を与えます。
:::

:::details 別解を見る

(b) については `Metric.mem_ball` で所属条件を展開し、`Real.dist_eq` を使えば集合としての等式も証明できます。

```lean:完成例・確認コマンド
import Mathlib

#check @Metric.mem_ball
-- Metric.mem_ball : x ∈ Metric.ball a ε ↔ dist x a < ε

-- 実数上の開球の集合としての等式も確認する
theorem real_ball_eq (a ε : ℝ) :
    Metric.ball a ε = {x : ℝ | |x - a| < ε} := by
  ext x
  simp only [Metric.mem_ball, Set.mem_setOf_eq, Real.dist_eq]
```

`Metric.ball a ε` の「メンバー判定」は `dist x a < ε` と完全に一致することが確認できます。
:::

 **問2（Lean）**

次は未完成の演習です。`sorry` を含むため警告が出ます。完成例とは別ファイルで実行し、穴を埋めてください。

```lean:未完成の骨格・練習問題
import Mathlib

-- 閉球は閉集合
theorem closedBall_exercise (X : Type*) [MetricSpace X] (x : X) (r : ℝ) :
    IsClosed (Metric.closedBall x r) := by
  sorry
```

ヒント：`Metric.isClosed_closedBall` という定理が Mathlib にあります（開球の定理が `Metric.isOpen_ball` であるのに対し、**閉**球の定理は `isClosed_closedBall` と命名されています）。別解では、連続写像による閉集合の逆像が閉集合になることを使います。

:::details ヒントを見る

`Metric.isClosed_closedBall` という定理が Mathlib にあります。開球の `Metric.isOpen_ball` に対応する、閉球版の定理です。
:::

:::details 解答を見る

```lean:完成例・確認コマンド
import Mathlib

-- 閉球は閉集合
theorem closedBall_closed (X : Type*) [MetricSpace X] (x : X) (r : ℝ) :
    IsClosed (Metric.closedBall x r) := by
  exact Metric.isClosed_closedBall
  -- No goals ✓
```

`Metric.isClosed_closedBall` は「`PseudoMetricSpace` において閉球は閉集合」という定理です。`MetricSpace` は `PseudoMetricSpace` を extends しているため、自動的に使えます。
:::

:::details 別解を見る

```lean:完成例・確認コマンド
import Mathlib

-- 距離関数の連続性と、閉集合の逆像を使う別解
theorem closedBall_closed_preimage (X : Type*) [MetricSpace X] (x : X) (r : ℝ) :
    IsClosed (Metric.closedBall x r) := by
  change IsClosed ((fun y : X => dist y x) ⁻¹' Set.Iic r)
  exact isClosed_Iic.preimage (continuous_id.dist continuous_const)
```

`change` は閉球の定義を使い、同じ集合を距離関数による $(-\infty,r]$ の逆像として書き直します。`continuous_id.dist continuous_const` が $y\mapsto d(y,x)$ の連続性、`isClosed_Iic.preimage` がその逆像の閉性を与えます。一般の距離空間ではノルムを仮定していないので、ここで使うのは距離関数です。
:::

 **問3（距離の公理）**

以下は「$d(x, y) = (x - y)^2$（差の2乗）は $\mathbb{R}$ 上の距離関数か？」という問いです。3公理（D1・D2・D3）のどれが成り立ち、どれが成り立たないかを確認し、Leanで反例をコードにしてください。

ヒント：`norm_num` や `ring` を活用してください。

:::details ヒントを見る

D3（三角不等式）に反例を探します。$x = 0,\; y = 1,\; z = 2$ として $(x-z)^2$ と $(x-y)^2 + (y-z)^2$ を比較してみてください。
:::

:::details 解答を見る

**分析：**
- D1（非負性）: $(x-y)^2 \geq 0$  ✓
- D1（零距離 → 同一点）: $(x-y)^2 = 0 \Rightarrow x = y$  ✓
- D2（対称律）: $(x-y)^2 = (y-x)^2$  ✓
- D3（三角不等式）: **✗**。反例 $x=0, y=1, z=2$: $(0-2)^2 = 4 > (0-1)^2 + (1-2)^2 = 2$

D3 が失敗するので、これは距離関数ではありません。

```lean:完成例・確認コマンド
import Mathlib

-- D3（三角不等式）の反例
theorem square_difference_not_metric : ¬ (∀ x y z : ℝ, (x - z)^2 ≤ (x - y)^2 + (y - z)^2) := by
  intro h
  have := h 0 1 2
  norm_num at this
  -- No goals ✓
```

`norm_num` が数値計算を自動的に処理して矛盾を導きます。
:::

:::details 別解を見る

```lean:完成例・確認コマンド
import Mathlib

-- 有理数の具体的な不等式を norm_num で否定する
theorem square_difference_rational_counterexample : ¬ ((0 - 2 : ℚ)^2 ≤ (0 - 1 : ℚ)^2 + (1 - 2 : ℚ)^2) := by
  norm_num
```

この別解は、有理数上の具体的な数値の反例を `norm_num` で確認しています。元の実数上の全称命題の否定は、直前の解答で証明しています。
:::

 **問4（発展）**

2点からなる型に、どの2点の距離も0となる擬距離を入れてください。その擬距離が距離空間の分離条件 $d(x,y)=0\Rightarrow x=y$ を満たさないことを証明してください。

:::details ヒントを見る

`PseudoMetricSpace.induced` は写像の値の間の距離を元の型に引き戻します。実数0への定数写像を使うと、すべての距離が0になります。
:::

:::details 解答を見る

次の擬距離は、距離空間の分離条件を満たしません。同じ2点集合に別の距離を入れることは可能なので、ここで否定するのはこの擬距離の分離条件です。

```lean:完成例・確認コマンド
import Mathlib

inductive TwoPoint | left | right

noncomputable instance : PseudoMetricSpace TwoPoint :=
  PseudoMetricSpace.induced (fun _ => (0 : ℝ)) inferInstance

theorem zeroDistanceDistinct :
    dist TwoPoint.left TwoPoint.right = 0 ∧ TwoPoint.left ≠ TwoPoint.right := by
  constructor
  · change dist (0 : ℝ) 0 = 0
    exact dist_self 0
  · intro h
    cases h
```

この例では `TwoPoint.left` と `TwoPoint.right` は異なりますが、距離は0です。距離0の点を同一視すれば、商は1点になります。なお $\mathbb{R}/\mathbb{Z}$ では0と1は同じ同値類です。また、ほとんど至る所等しい関数を同一視した後の $L^2$ は距離空間です。これらを「商を取った後も別の点なのに距離0」という例に使うことはできません。
:::

:::details 別解を見る

上で定義した `TwoPoint` と擬距離を使い、分離条件そのものを否定できます。次のブロックは上のコードの後に置きます。

```lean:完成例・確認コマンド
theorem zeroDistanceNotSeparating :
    ¬ (∀ x y : TwoPoint, dist x y = 0 → x = y) := by
  intro h
  exact zeroDistanceDistinct.2 (h _ _ zeroDistanceDistinct.1)
```

`PseudoMetricSpace.induced` は `Mathlib.Topology.MetricSpace.Pseudo.Constructions` にあります。任意の `Quotient` に距離が自動で入るわけではなく、商上の距離が定義できるか、分離条件を満たすかを個別に確認する必要があります。
:::
:::

---

:::message
**📝 第7章 つまずきポイントQ&A**

**Q1. `MetricSpace` のインスタンスはどこで定義されているの？**
→ Mathlib は `ℝ`、`ℂ`、`EuclideanSpace` などのインスタンスを登録しています。定理の宣言で `[MetricSpace X]` と書くと、その構造を前提として受け取ります。具体的な型に定理を適用する際は、型クラス探索が利用可能なインスタンスを補います。

**Q2. `dist x y` を直接計算したいのですが、`ℝ` の場合は何になりますか？**
→ `dist x y = |x - y|` です。`Real.dist_eq` というレンマが `dist x y = |x - y|` を述べており、`simp [Real.dist_eq]` で展開できます。

**Q3. `Metric.ball` と `Metric.closedBall` の違いは何ですか？**
→ `Metric.ball x r = {y | dist y x < r}` と `Metric.closedBall x r = {y | dist y x ≤ r}` です。前者は距離がちょうど $r$ の点を含まず、後者は含みます。一般の距離空間では、閉球が開球の閉包と一致するとは限りません。

**Q4. 三角不等式を Lean で使うにはどう書きますか？**
→ `dist_triangle x y z : dist x z ≤ dist x y + dist y z` が使えます。`calc` ブロックと組み合わせるのが読みやすいです。

**Q5. `MetricSpace` と `PseudoMetricSpace` は何が違いますか？**
→ どちらも `dist x x = 0` を満たします。`MetricSpace` はさらに `dist x y = 0 → x = y` を要求し、`PseudoMetricSpace` では異なる点の距離が0でも構いません。必要な補題がどちらの構造を仮定するかを確認して使います。
:::

> **著者より**：距離の3公理を Lean に打ち込んだ瞬間、「数学の授業で黒板に書いた公理が、型チェッカーにそのまま認識される」という体験に軽く感動しました。数学は仕様書であり、Lean はそのコンパイラです。[第8章](ch08)では、この「距離」をさらに抽象化した「位相」を見ていきます。



