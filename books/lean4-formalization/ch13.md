---
title: "第13章：測度──長さ・面積の一般化"
---

# 第13章：測度空間の建設 〜「大きさ」を公理から作る〜

以下の完成例は、各ブロックの `import` と宣言を含めて、それぞれ別のLeanファイルで実行できます。演習の `sorry` は未完成の骨格です。

## 13.0 本章のゴール ──── ゴール逆算型アプローチ

まず本章の終着点を見ていきます。以下のコードが理解・実行できることが本章のゴールです。

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

-- 一般の a,b では ENNReal.ofReal (b-a)。a ≤ b なら区間の長さに対応する。
#check @Real.volume_Icc
#check @Real.volume_Ioo
#check @Real.volume_singleton

theorem volume_unit_interval : volume (Set.Icc (0 : ℝ) 1) = 1 := by
  simp [Real.volume_Icc]

#check @IsProbabilityMeasure
#check @prob_le_one

theorem dirac_is_probability : IsProbabilityMeasure (Measure.dirac (0 : ℝ)) :=
  inferInstance
```

「なぜ `Real.volume_Icc` は測度の値を `ENNReal.ofReal (b - a)` で表すのか」「`IsProbabilityMeasure` はどう使うのか」——これらを一つずつ解きほぐしていきます。

---

## 13.1 測度の直観 ──── 「σ-代数に大きさを割り当てる関数」

![可測集合に非負拡張実数の値を割り当てる測度の模式図](/images/vol3/fig-04.png)
*図13-1：数学での可測集合上の測度を表します。Mathlibの`Measure`は可測でない集合にも値を持ち、可算加法性などには可測性の仮定が使われます。*

[第12章](ch12)で「どの集合が測れるか」を決める σ-代数を作りました。次は「測れる集合に具体的な大きさを割り当てる」関数を作ります。これが**測度**です。

直観的には次のことを要求したいです。

- 大きさは $0$ 以上（負の大きさはない）
- 空集合の大きさは $0$
- バラバラな集合の大きさは「足し算できる」

最後の条件をもう少し丁寧に言うと：互いに素な可算個の**可測集合** $A_1, A_2, \ldots$ に対して、

$$
\mu\!\left(\bigsqcup_{n=1}^\infty A_n\right) = \sum_{n=1}^\infty \mu(A_n)
$$

が成り立ってほしい、ということです。これが**可算加法性**（σ-加法性）です。

---

## 13.2 測度の公理 ──── 値域と二つの条件

### 数学の定義：測度

可測空間 $(X, \mathcal{M})$ 上の**測度**とは、関数 $\mu : \mathcal{M} \to [0, +\infty]$ であって次を満たすものです。

$$
\text{(M1)}\quad \mu(\emptyset) = 0
$$

$$
\text{(M2)}\quad A_1, A_2, \ldots \in \mathcal{M} \text{ が互いに素} \Rightarrow \mu\!\left(\bigsqcup_{n=1}^\infty A_n\right) = \sum_{n=1}^\infty \mu(A_n)
$$

値域が $[0, +\infty]$ であること、つまり $+\infty$ も許していることに注目してください。$\mathbb{R}$ 全体の Lebesgue 測度は $+\infty$ です。

組 $(X, \mathcal{M}, \mu)$ を**測度空間**と呼びます。

:::message
💡 **「互いに素」の重要性**

可算加法性は「互いに素な集合族」に対して要求されます。重なりがある場合は $\mu(A \cup B) \leq \mu(A) + \mu(B)$（劣加法性）が成り立ちますが、一般には等号を保証できません。重なりの測度が0など、互いに素でなくても等号になる場合はあります。
:::

---

## 13.3 `Measure α` を解剖します ──── OuterMeasure との関係

### Lean の実装：`MeasureTheory.Measure`

`Measure α` の構造体を `#print` で覗いてみましょう。

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

#check @Measure
#print Measure
#check @Measure.trimmed
#check @measure_eq_iInf
```

以下は `#print` の内容を、継承元をまとめた宣言の形に整理したものです。

```text
structure Measure (α : Type*) [MeasurableSpace α] extends OuterMeasure α where
  m_iUnion : ∀ {f : ℕ → Set α},
    (∀ i, MeasurableSet (f i)) → Pairwise (Disjoint on f) →
    toOuterMeasure (⋃ i, f i) = ∑' i, toOuterMeasure (f i)
  trim_le : toOuterMeasure.trim ≤ toOuterMeasure
```

`Measure α` の正体は、**外測度 `OuterMeasure α` を継承する構造体**です。フィールドを一つずつ見ていきます。

| フィールド名 | 数学的意味 |
|---|---|
| `toOuterMeasure` | 全ての部分集合で評価できる外測度。空集合・単調性・可算劣加法性の性質も継承する |
| `m_iUnion` | 互いに素な可測集合族の可算加法性（条件 M2） |
| `trim_le` | 可測集合上の値から作る標準的な外測度の拡張との整合性条件 |

:::message
💡 **なぜ `OuterMeasure` を経由するのか**

外測度は全ての部分集合上に定義され、空集合の値が0で、単調性と可算劣加法性を持ちます。任意の集合族について可算加法的であるとは限りません。外測度から測度を作る方法の一つは、Carathéodoryの条件を満たす集合族へ制限する方法です。任意のσ-代数へ制限すれば自動的に測度になる、という意味ではありません。

Mathlibでは、`μ : Measure α` も全ての `s : Set α` に対して `μ s` と評価できます。非可測集合での値は外測度としての値です。`measure_eq_iInf` は、`s` を含む可測集合の測度の下限としてその値が決まることを述べます。`trim_le` と外測度の一般的な不等式から得る等式が `Measure.trimmed` であり、`trimmed` 自体は構造体のフィールド名ではありません。
:::

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

#check @Measure.toOuterMeasure
#check @measure_empty
#check @measure_iUnion
#check @measure_mono
#check @measure_union_le

theorem countable_additivity_demo {α : Type*} [MeasurableSpace α]
    (μ : Measure α) (f : ℕ → Set α)
    (hd : Pairwise (fun i j => Disjoint (f i) (f j)))
    (hm : ∀ i, MeasurableSet (f i)) :
    μ (⋃ i, f i) = ∑' i, μ (f i) :=
  measure_iUnion hd hm
```

公開補題 `measure_iUnion` の順序は「互いに素 `hd`、各集合の可測性 `hm`」です。一方、構造体のフィールド `m_iUnion` は「可測性、互いに素」の順です。`measure_empty`・`measure_mono` は可測性の仮定なしに使えます。

---

## 13.4 具体例三選 ──── Lebesgue・数え上げ・Dirac

| 測度 | 集合への値 | この例での条件 |
|---|---|---|
| Lebesgue測度 | $[a,b]$ の測度は $b-a$ | 実数上、$a\leq b$ |
| 数え上げ測度 | 点の個数。無限集合では $\infty$ | 全ての部分集合が可測な離散可測空間 |
| 点 $a$ のDirac測度 | $a\in s$ なら1、そうでなければ0 | $s$ は可測集合 |

可算加法性は3種類すべての測度に共通します。

### 例1：Lebesgue 測度

実数直線上で「区間の長さ」を一般化したものが Lebesgue 測度です。

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

-- Lebesgue 測度 volume : Measure ℝ
#check (volume : Measure ℝ)
-- volume : MeasureTheory.Measure ℝ
```

`volume` の型は `Measure ℝ` です。Mathlibの標準の `MeasurableSpace ℝ` はボレルσ-代数です。その上の `volume` がLebesgue測度を表し、任意の部分集合で評価するときは対応する外測度の値を返します。数学でいうLebesgue可測集合の全てが、この標準構造で `MeasurableSet` を満たすわけではありません。零集合の部分集合まで含める完備化とは区別します。

#### 証明の実況中継：$\lambda([0,3]) = 3$ を確認する

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

-- 目標：volume (Set.Icc (0 : ℝ) 3) = 3
theorem volume_zero_three : volume (Set.Icc (0 : ℝ) 3) = 3 := by
  rw [Real.volume_Icc]    -- ステップ②
  norm_num                 -- ステップ③
-- No goals ✓
```

**ステップ①：ゴールを確認する**

```text
⊢ volume (Set.Icc 0 3) = 3
```

**ステップ②：`rw [Real.volume_Icc]` を使う**

`Real.volume_Icc` は一般の端点に対して `volume (Set.Icc a b) = ENNReal.ofReal (b - a)` を与える補題です。ゴールが `ENNReal.ofReal` を含む形に書き換わります。

```text
⊢ ENNReal.ofReal (3 - 0) = 3
```

**ステップ③：`norm_num` で数値計算を閉じる**

`3 - 0 = 3` かつ `ENNReal.ofReal 3 = 3` を数値計算で確認します。

```text
No goals
```

`-- No goals ✓`

:::message
💡 **`ENNReal.ofReal` は何者か**

`volume` がとる値の型は `ℝ≥0∞`（= `ENNReal`、拡張非負実数）です。`ENNReal.ofReal r` は通常の実数 $r \geq 0$ を `ℝ≥0∞` に埋め込む関数で、$r < 0$ の場合は $0$ を返します。Lebesgue 測度が $[0, +\infty]$ に値をとるため、この型が必要になります。`a > b` なら区間は空集合であり、`ofReal (b-a) = 0` と整合します。非負積分の値域としても[第16章](ch16)で使います。
:::

その他の基本性質も確認しておきましょう。

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

-- 開区間でも端点の差（端点の差は変わらない）
theorem volume_open_unit_interval : volume (Set.Ioo (0 : ℝ) 1) = 1 := by
  simp [Real.volume_Ioo]
  -- No goals ✓

-- 一点集合の測度は 0（有理数・無理数にかかわらず）
-- 数学：λ({a}) = 0 / Lean：Real.volume_singleton
theorem volume_singleton_zero (a : ℝ) : volume ({a} : Set ℝ) = 0 := Real.volume_singleton
-- No goals ✓
```

### 例2：数え上げ測度

有限集合には要素の個数を、無限集合には $∞$ を返す測度です。一点集合が可測な空間で、この説明がそのまま成り立ちます。ここでは自然数上の標準の可測構造を使います。

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

-- 数え上げ測度 Measure.count
#check @Measure.count
-- Measure.count : Measure α

-- 単集合の数え上げ：一点集合の数は 1
-- 数学：#({a}) = 1 / Lean：Measure.count_singleton
theorem count_singleton_one : Measure.count ({5} : Set ℕ) = 1 := Measure.count_singleton 5
-- No goals ✓

-- 空集合の数え上げ：0 個
theorem count_empty_zero : Measure.count (∅ : Set ℕ) = 0 := by simp
-- No goals ✓
```

:::message
💡 **数え上げ測度と Lebesgue 測度の違い**

Lebesgue 測度では一点集合の測度が $0$ ですが、数え上げ測度では $1$ です。同じ集合でも「どの測度を使うか」によって大きさが変わります。測度空間 $(X, \mathcal{M}, \mu)$ において $\mu$ の選択が本質的です。
:::

### 例3：Dirac 測度

1点に全重量を集中させた測度です。可測集合 `s` での値は、その点が `s` に含まれれば1、含まれなければ0です。実数では一点集合が可測なので、この式は任意の部分集合についても成り立ちます。

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

-- Dirac 測度 δ_a：点 a に全重量を集中させる
-- 数学：δ_a(A) = 1 if a ∈ A, 0 if a ∉ A / Lean：Measure.dirac
#check @Measure.dirac
-- Measure.dirac : α → Measure α

-- δ₀({0}) = 1（0 は {0} に属する）
theorem dirac_at_zero : Measure.dirac (0 : ℝ) {0} = 1 := by
  simp [Measure.dirac_apply']
  -- No goals ✓

-- δ₀({1}) = 0（0 は {1} に属さない）
theorem dirac_away_from_zero : Measure.dirac (0 : ℝ) {1} = 0 := by
  simp [Measure.dirac_apply']
  -- No goals ✓
```

---

## 13.5 確率測度 ──── Part 4（予定）への布石

### 数学の定義：確率測度

測度空間 $(X, \mathcal{M}, \mu)$ において $\mu(X) = 1$ が成り立つとき、$\mu$ を**確率測度**と呼びます。

### Lean の実装：`IsProbabilityMeasure`

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

-- IsProbabilityMeasure の定義
#print IsProbabilityMeasure
-- class IsProbabilityMeasure (μ : Measure α) : Prop where
--   measure_univ : μ Set.univ = 1

-- Dirac測度は確率測度（自動的にインスタンスが解決される）
theorem dirac_probability_demo : IsProbabilityMeasure (Measure.dirac (0 : ℝ)) := inferInstance

-- 確率測度では任意の集合の測度は [0,1] に収まる
theorem probability_bound (μ : Measure ℝ) [IsProbabilityMeasure μ] (s : Set ℝ) :
    μ s ≤ 1 := prob_le_one
```

`IsProbabilityMeasure` という型クラスの免許証を持つことで、`prob_le_one` などの確率測度専用の補題が自動的に使えるようになります。

:::message
🔗 **Part 4（予定）への布石**

確率論では、$\Omega$ を標本空間、$\mathcal{F}$ を事象の σ-代数、$P$ を確率測度として、三つ組 $(\Omega, \mathcal{F}, P)$ を**確率空間**と呼びます。Lean では次のように書きます。

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory
section Example10

variable (Ω : Type*) [MeasurableSpace Ω]
         (P : Measure Ω) [IsProbabilityMeasure P]

end Example10
```

この型クラスによる設定が、Part 4（予定）で martingale や Itô 積分を形式化する際の出発点になります。
:::

---

## 13.6 まとめ ──── 第13章の全対応表と練習問題

数学とLeanの対応を表にまとめます。

| 数学の表現 | Lean の表現 |
|---|---|
| $(X, \mathcal{M}, \mu)$ が測度空間 | `[MeasurableSpace X]` + `μ : Measure X` |
| $\mu(\emptyset) = 0$（条件M1） | `measure_empty` |
| $\mu(\bigsqcup_n A_n) = \sum_n \mu(A_n)$（条件M2） | `measure_iUnion` |
| $A \subseteq B \Rightarrow \mu(A) \leq \mu(B)$ | `measure_mono` |
| Lebesgue 測度 $\lambda$ | `volume : Measure ℝ` |
| $a \le b$ のとき $\lambda([a,b]) = b-a$ | `Real.volume_Icc` |
| $\lambda(\{a\}) = 0$ | `Real.volume_singleton` |
| Dirac 測度 $\delta_a$ | `Measure.dirac a` |
| 数え上げ測度 | `Measure.count` |
| $\#\{a\} = 1$ | `Measure.count_singleton` |
| 確率測度 $\mu(X) = 1$ | `IsProbabilityMeasure μ`（型クラスの免許証） |
| $\mu(A) \leq 1$（確率測度） | `prob_le_one` |

### 章末練習問題

**問題 13.1**　Lebesgue 測度について `volume (Set.Icc (1 : ℝ) 4) = 3` を証明してください。`Real.volume_Icc` と `norm_num` を使うと証明できます。


:::details ヒントを見る

`Real.volume_Icc` と `norm_num` を組み合わせてください。`Real.volume_Icc` は `volume (Set.Icc a b) = ENNReal.ofReal (b - a)` を述べます。
:::

:::details 解答を見る

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory
theorem volume_one_four : volume (Set.Icc (1 : ℝ) 4) = 3 := by
  rw [Real.volume_Icc]
  norm_num
  -- No goals ✓
```

`Real.volume_Icc` で `ENNReal.ofReal (4 - 1)` に書き換え、`norm_num` が `ENNReal.ofReal 3 = 3` を自動計算します。
:::

:::details 別解を見る

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory
theorem volume_one_four_alt : volume (Set.Icc (1 : ℝ) 4) = 3 := by
  simp [Real.volume_Icc, show (4:ℝ) - 1 = 3 from by norm_num]
```

`simp` に補題名と等式ヒントを渡して一括処理する別解です。
:::

**問題 13.2**　測度の単調性 `measure_mono` を使って `volume (Set.Icc (0:ℝ) 1) ≤ volume (Set.Icc (0:ℝ) 2)` を証明してください。

```lean:未完成の骨格・練習問題
import Mathlib
open MeasureTheory

-- 演習の穴。完成例とは別ファイルで実行する。
theorem measure_mono_exercise :
    volume (Set.Icc (0 : ℝ) 1) ≤ volume (Set.Icc (0 : ℝ) 2) := by
  apply measure_mono
  sorry
```


:::details ヒントを見る

`Set.Icc_subset_Icc` は `ha : a' ≤ a` と `hb : b ≤ b'` を取って `Set.Icc a b ⊆ Set.Icc a' b'` を返します。今回は左端は同じなので `le_refl` を使います。
:::

:::details 解答を見る

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory
theorem interval_measure_mono : volume (Set.Icc (0 : ℝ) 1) ≤ volume (Set.Icc (0 : ℝ) 2) := by
  apply measure_mono
  exact Set.Icc_subset_Icc le_rfl (by norm_num)
  -- No goals ✓
```

`measure_mono` で「集合の包含関係 → 測度の不等式」を適用し、`Set.Icc_subset_Icc` で $[0,1] \subseteq [0,2]$ を証明します。
:::

:::details 別解を見る

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory
theorem interval_measure_mono_alt : volume (Set.Icc (0 : ℝ) 1) ≤ volume (Set.Icc (0 : ℝ) 2) := by
  apply measure_mono
  intro x ⟨hx0, hx1⟩
  exact ⟨hx0, by linarith⟩
```

集合の包含を `intro x` で展開し、メンバーシップの条件を `linarith` で処理する計算的な別解です。
:::

**問題 13.3**　`Measure.restrict` を使って、Lebesgue 測度を $[0, 1]$ に制限した測度 `volume.restrict (Set.Icc 0 1)` が `IsProbabilityMeasure` になることを確認してください（`Real.volume_Icc` と `IsProbabilityMeasure.mk` を活用します）。


:::details ヒントを見る

`IsProbabilityMeasure.mk` は `μ Set.univ = 1` の証明を要求します。`Measure.restrict_apply` と `Real.volume_Icc` を組み合わせて `1` が出ることを示してください。制限測度の台の型は引き続き `ℝ` であり、その全体集合の測度が元の `[0,1]` の測度になります。
:::

:::details 解答を見る

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory
theorem restricted_volume_probability : IsProbabilityMeasure (volume.restrict (Set.Icc (0:ℝ) 1)) := by
  constructor
  rw [Measure.restrict_apply MeasurableSet.univ, Set.univ_inter]
  rw [Real.volume_Icc]
  norm_num
  -- No goals ✓
```

`Measure.restrict_apply` で制限測度の全体を元の測度に戻し、`Real.volume_Icc` で $\lambda([0,1]) = 1$ を計算します。
:::

:::details 別解を見る

```lean:完成例・確認コマンド
import Mathlib
open MeasureTheory

theorem restricted_volume_probability_alt :
    IsProbabilityMeasure (volume.restrict (Set.Icc (0 : ℝ) 1)) := by
  refine ⟨?_⟩
  simp [Real.volume_Icc]
```

`IsProbabilityMeasure` の唯一の条件を `refine ⟨?_⟩` で取り出し、制限測度の全体集合での値を `simp` で計算します。
:::

この[0,1]上の確率測度は、[第18章の総合演習](ch18)で再び使います。そこで $X(x)=x$ の期待値を求め、同じ関数から $L^2$ の元を作ります。

:::message
**📝 第13章 つまずきポイントQ&A**

**Q1. `measure_union` に `Disjoint` が必要な理由は何ですか？**
→ 可測性の条件に加え、互いに素であることは、重なりの二重計算を避けて加法性を使う十分条件です。重なりが零集合なら等号が成り立つ場合もあり、必要条件とは限りません。`measure_union_le` は `Disjoint` 抜きで `≤` だけを言う弱い補題です。

**Q2. `ENNReal.ofReal` と `NNReal.toReal` の使い分けは？**
→ `ENNReal.ofReal : ℝ → ℝ≥0∞` は通常の実数を拡張非負実数に変換します（負は 0 に丸める）。`NNReal.toReal : ℝ≥0 → ℝ` は非負実数を通常の実数に戻します。測度の計算では `ENNReal.ofReal` が頻出します。

**Q3. `Measure.restrict` の使い方を教えてください**
→ `μ.restrict s` は「集合 `s` の外を無視した測度」です。`Measure.restrict_apply (hs : MeasurableSet t) : (μ.restrict s) t = μ (t ∩ s)` が基本補題です。

**Q4. `IsProbabilityMeasure` と `IsFiniteMeasure` の違いは？**
→ `IsFiniteMeasure μ` は `μ Set.univ < ⊤`（全体の測度が有限）です。`IsProbabilityMeasure μ` は `μ Set.univ = 1` です。確率測度は有限測度ですが、その逆は成り立ちません。

**Q5. `volume` は常にLebesgue測度ですか？**
→ `volume` は、その型の `MeasureSpace` が指定するデフォルト測度です。実数の標準インスタンスではLebesgue測度ですが、全ての型で同じ意味になるわけではありません。別の測度を使うときは `μ : Measure α` と明示します。
:::

### 著者より

`Real.volume_Icc` が測度の値を `ENNReal.ofReal (b - a)` で表すのを初めて見たとき、「なぜ普通の実数じゃないのか」と戸惑いました。[第16章](ch16)で `lintegral` の値域が `ℝ≥0∞` であることを学ぶと、この設計の一貫性が見えてきます。測度論の型設計は深いです。

---

