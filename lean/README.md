# Lean 4形式化本の検証環境

段階5までに、導入と第1〜18章の本文・掲載コード・演習・Q&A、および全体の文章・構成・Zenn表示を修正しました。新しい環境からの再現と最終確認は段階6で行います。検証環境と対応表の対象は `books/lean4-formalization` の全23本のMarkdownです。初回の421ブロックに説明用の例と確認コマンドを4個追加し、現在は425ブロックを記録しています。各段階の修正内容と検証範囲は、下記の実施記録に保存しています。

## 採用する版

| 対象 | 固定値 |
|---|---|
| 原稿レビューの基準 | `918e65ab659ad2750b50bef0f28891dbccf070e4` |
| Lean | `4.29.0-rc6` |
| Mathlib | `5c8398df528176d9c87ccd9226ba8f7c8852d59c` |
| 間接依存 | `lake-manifest.json` の各コミット |

既存の修正例を検証した組み合わせを、この修正作業と掲載コードの基準に採用します。Leanはリリース候補版です。最新版・安定版を意味しません。別の版への更新時は、Lean、Mathlib、依存関係の記録を一緒に更新し、掲載する完成例を再検証します。

## 再現手順

Git、Python 3.10以降、elanを利用できる状態で、このリポジトリの `lean/` に移動します。

```sh
cd lean
lake exe cache get
python3 scripts/catalog.py extract
python3 scripts/verify.py
```

elanは `lean-toolchain` に書かれた版を使用します。初回の依存関係とビルドキャッシュの取得にはネット接続とディスク容量が必要です。`lake exe cache get` はMathlibのビルド済みファイルを取得します。原稿の検証は続く `verify.py` が行います。通常の検証で `lake update` は実行しません。

`verify.py` は原稿との対応、取得済み依存リポジトリのコミットと追跡ファイルの変更、Leanの版を確認し、`lake build` と `Audit.lean` の公理検査を実行します。さらに、原稿から抽出した例のエラー・警告・公理一覧を照合します。記録は `.generated/verification.json` と `.generated/logs/` に保存されます。検査に失敗したときは成功記録を残しません。

対応表だけを確認する場合は、LeanやMathlibの取得前でも次を実行できます。

```sh
python3 scripts/catalog.py check
```

検査スクリプト自体の変更時は `python3 scripts/test_validation.py` も実行します。コードの欠落・変更や、文脈参照の欠落、未確認の公理を成功扱いしないことを確認します。

## 現在の実行検証範囲（段階4-C）

| ファイル | 内容 |
|---|---|
| `LeanBook/Chapters/Intro.lean` | 確率空間の前提の宣言：1ブロック |
| `LeanBook/Chapters/Ch01.lean` | 環境確認、最初の証明、演習の完成解答：11ブロック |
| `LeanBook/Chapters/Ch02.lean` | 型と証明項、自然数から整数への変換、型クラス、演習解答：17ブロック |
| `LeanBook/Chapters/Ch03.lean` | エラーの修正例、完成した証明、可換モノイドと偶数性の演習解答：14ブロック |
| `LeanBook/Chapters/Ch04.lean` | 型の確認、探索、名前空間、タクティク、演習解答：19ブロック |
| `LeanBook/Chapters/Ch04Imports.lean`・`Ch04MinImports.lean` | 個別importとimport候補の探索を別ファイルで検証：各1ブロック |
| `LeanBook/Chapters/Ch05.lean` | 仮定の宣言、核と像の有限次元性、次元定理の適用、演習解答：26ブロック |
| `LeanBook/Chapters/Ch06.lean` | 連続性・フィルター・近傍・極限・演習解答：45ブロック（証明の断片を含む） |
| `LeanBook/Chapters/Ch07.lean` | 距離・近傍の基底・開球・閉球・擬距離・演習解答：40ブロック（証明の断片を含む） |
| `LeanBook/Chapters/Ch08.lean` | 位相・近傍・合成・コンパクト性・極値・演習解答：34ブロック（証明の断片を含む） |
| `LeanBook/Chapters/Ch09.lean` | Cauchy性・完備性・一意な延長・演習解答：26ブロック（証明の断片を含む） |
| `LeanBook/Chapters/Ch10.lean` | ℓ²の定義・完備性・基底・非コンパクト性・級数の演習解答：25ブロック（証明の断片を含む） |
| `LeanBook/Chapters/Ch11.lean` | 不動点の一意存在・具体値・反復収束：24ブロック（証明の断片を含む） |
| `LeanBook/Chapters/Ch12.lean` | 不動点の再掲、可測空間の公理、ボレル構造、演習解答：18ブロック |
| `LeanBook/Chapters/Ch13.lean` | 測度の構造・補題、区間・数え上げ・Dirac測度、確率測度の演習：16ブロック |
| `LeanBook/Chapters/Ch14.lean` | 可測性・AE可測性・測度の制限・一点での変更・演習解答：15ブロック |
| `LeanBook/Chapters/Ch15.lean` | 単関数の構造・値域・定数積分・近似・指示関数・演習：12ブロック |
| `LeanBook/Chapters/Ch16.lean` | 非負積分の定義、可積分性と規約値、ディリクレ・正弦・指数関数、積分公式・演習：17ブロック |
| `LeanBook/Chapters/Ch17.lean` | 収束定理の仮定と適用、Fatouの真の不等式、正弦関数列の積分収束：全13ブロック |
| `LeanBook/Chapters/Ch18.lean` | Lᵖの指数・AE強可測性、確率測度・期待値・条件付き期待値・マルチンゲール、L²の内積：全15ブロック |
| `LeanBook/Baseline.lean` | レビューで検証した修正例：連続性、不動点、積分、収束定理など |
| `Audit.lean` | 上記で名前を付けた309宣言の公理依存 |

掲載コードのビルド対象は計390ブロックで、完成例341と断片49のすべてに対応します。途中の断片は指定された順に連結します。章別ファイルは原稿のimportを維持し、名前の衝突を防ぐ名前空間を追加しています。`BEGIN SOURCE` と `END SOURCE` の間は原稿からの抽出内容と照合します。Baselineは今後の修正に使う参照例で、現在の本文と一致するという意味ではありません。

警告をビルドエラーとして扱います。主要な宣言については `sorryAx` だけでなく、未確認の公理への依存も検出します。許容する標準公理は `propext`、`Classical.choice`、`Quot.sound` で、各宣言が使うものをログに記録します。検査対象の追加時には `Audit.lean` も更新します。

`catalog/index.json` の `diagnostics` に37件の照合対象を記録しています。偽の等式は指定した2件のエラーが出ること、`sorry` の例は警告と `sorryAx` が出ること、完成例は掲載した公理一覧が得られることを確認します。第1〜5章の意図したエラー9件・未完成13件、完成例の公理2件に加え、第11章の連続性の仮定不足による失敗と、縮小性の演習の穴も確認します。第6・7章の意図したエラー2件と未完成の演習2件も加えています。第8・9章の未完成の演習4件も加えています。第10章の級数の演習の穴1件も加えています。第12・13章の演習の穴2件も加えています。単にコンパイラが失敗しただけでは検査の成功としません。

これらは原稿のコードを抽出し、それぞれ独立したLeanプロセスで実行します。未完成例には警告を観察するため `-DwarningAsError=false` を指定しますが、完成例のビルドと公理検査の基準は変えません。未証明の宣言を `LeanBook` の完成例へimportしません。

導入と第1〜5章の完成・連結例90ブロックは、85組の独立したLeanプロセスでも検証しています。`python3 scripts/check_stage2_sources.py` で再現でき、結果は `.generated/stage2-source-checks.json` に出力します。個別importの例では `import Mathlib` を補わず、連結の例は指定した順だけで実行します。

第6・7章の完成・連結例85ブロックは、61組の独立したLeanプロセスでも検証しています。`python3 scripts/check_stage3a_sources.py` で再現でき、結果は `.generated/stage3a-source-checks.json` に出力します。完成例のimportと明記した連結順を使い、名前付き宣言の公理も照合します。

第8・9章の完成・連結例60ブロックは、47組の独立したLeanプロセスでも検証しています。`python3 scripts/check_stage3b_sources.py` で再現でき、結果は `.generated/stage3b-source-checks.json` に出力します。各例のimport・連結順・公理を確認します。

第10・11章の完成・連結例49ブロックは、42組の独立したLeanプロセスでも検証しています。`python3 scripts/check_stage3c_sources.py` で再現でき、結果は `.generated/stage3c-source-checks.json` に出力します。必要な原稿の前提を `setup`、検証対象を `blocks` に分け、前提の再利用を件数に加算しません。

第12〜14章の完成例49ブロックは、それぞれ独立したLeanプロセスでも検証しています。`python3 scripts/check_stage4a_sources.py` で再現でき、結果は `.generated/stage4a-source-checks.json` に出力します。各ブロック自身のimportと前提だけを使います。

第15・16章の29ブロックと第17章の関連例 `ch17_010` は、30組の独立したLeanプロセスでも検証しています。`python3 scripts/check_stage4b_sources.py` で再現でき、結果は `.generated/stage4b-source-checks.json` に出力します。前の定理を使う補足2件は、原稿で明示した前提を `setup` に記録し、再利用を対象件数に加算しません。

第17・18章の全28ブロックは、28組の独立したLeanプロセスでも検証しています。`python3 scripts/check_stage4c_sources.py` で再現でき、結果は `.generated/stage4c-source-checks.json` に出力します。第17章の2つの続きは明示した前提を `setup` に記録し、第18章は各例自身のimport・前提だけで実行します。

**ビルドの成功は425ブロックすべての動作確認を意味しません。** 意図的なエラー、演習の穴、説明用の抜粋、途中のタクティクは一括ビルドに混ぜず、以下の分類で扱います。数学的な主張と形式化した主張の一致、文章の正確さも別途確認します。

## 原稿と修正計画の対応

- [段階5の文章・図・表示の修正記録](STAGE5.md)（Zennの検査・プレビュー手順と確認の限界も記載）
- [段階4-Cの修正・検証記録](STAGE4C.md)
- [段階4-Bの修正・検証記録](STAGE4B.md)
- [段階4-Aの修正・検証記録](STAGE4A.md)
- [段階3-Cの修正・検証記録](STAGE3C.md)
- [段階3-Bの修正・検証記録](STAGE3B.md)
- [段階3-Aの修正・検証記録](STAGE3A.md)
- [段階2の修正・検証記録](STAGE2.md)
- [段階1-Dの修正・検証記録](STAGE1D.md)
- [段階1-Cの修正・検証記録](STAGE1C.md)
- [段階1-Bの修正・検証記録](STAGE1B.md)
- [段階1-Aの修正・検証記録](STAGE1A.md) / [段階0の実施記録](STAGE0.md)
- [全425ブロックの分類表](catalog/README.md)
- [章別データの索引](catalog/index.json)・[各章の前提・挿入位置・検証先](catalog/chapters/)
- [レビュー指摘と修正段階の対応表](catalog/ISSUES.md)
- [指摘の機械可読データ](catalog/issues.json)

| 区分 | 今後の扱い |
|---|---|
| 完成例・実行コマンド | 原稿で示すimportと前提で実行し、出力を照合する。完成解答には未証明部分を残さない。 |
| 前後に続く断片 | `context` に指定した宣言・証明位置へ組み込み、段階ごとのゴールと完成形を確認する。 |
| 意図的なエラー例 | 想定した理由でエラーになることを確認し、説明と対応させる。 |
| 練習問題・未完成の骨格 | 未完成であることを表示し、完成解答と別に管理する。 |
| 説明用の抜粋 | 実際の定義・型・出力と照合し、省略を明示する。 |

分類は掲載目的を表します。不備のある完成解答を未完成例に分類し直して、修正対象から外すことはしません。各ブロックの `fixture` は原文を保存するファイルで、検証成功を保証するものではありません。断片の `context.blocks` は文脈や置換位置の参照です。同じ証明を説明する複数の段階を単純に連結する指示ではありません。`preceding_setup` は先行するimport/openの位置情報で、先行例の局所変数が有効だと保証する情報ではありません。

初回421ブロックの一次点検は各ブロックに `import Mathlib` を補って独立実行したものです。正常終了232件のうち24件にsorry警告、エラー189件でした。変更したコードの過去の点検結果には元のIDと内容ハッシュを添え、新しいコードの動作確認と混同しないようにしています。この記録は切り分けの参考に限り、証明数や原稿の誤りの件数として扱いません。

## 段階1以降の更新方法

1. `catalog/issues.json` から対象指摘を選び、本文・予告・再掲・演習を合わせて修正します。
2. 完成例を章別Leanファイルに追加し、`LeanBook.lean` にimportします。原稿と対応する宣言には `compiled_mappings` とソースマーカーを付けます。
3. 原稿の変更に合わせ、`catalog/chapters/章名.json` の位置・内容ハッシュ・分類・文脈を確認して更新します。原稿全体のハッシュ、章別ファイル一覧、ビルド対象との対応は `catalog/index.json` に記録します。ブロックの追加や削除でIDが変わる場合、文脈参照と指摘の参照も更新します。自動的に成功扱いへ変更しません。
4. `catalog.py extract` で原文の抽出先と表を再生成し、`verify.py` で検証します。必要な主要宣言を `Audit.lean` に追加します。
5. 指摘の完了条件を満たしたものだけ、解消した変更と検証記録を添えて状態を更新します。

本文の変更やコードの取り違えを見逃さないよう、対応表はMarkdown全23本のハッシュとコードごとのハッシュを検査します。本文だけの変更でも確認が必要です。段階6では全完成例と意図的なエラーを含む最終検証、Zennの全ページ表示確認、継続的な自動検査を揃えます。
